# Clo4 Backend - Developed with Node.js(Express) + MongoDB

## Project Running

### Install dependencies
`npm install`

### Run the server locally
`npm run start`

## To start server with hot reload 
`npm run dev`